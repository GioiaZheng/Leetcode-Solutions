# LeetCode <ID> – <Problem Title>

**Difficulty:** <Easy | Medium | Hard>
**Tags:** <Topic 1, Topic 2>
**Link:** <https://leetcode.com/problems/.../>

---

## Problem

Briefly restate the problem in your own words, including the required return value.

---

## Intuition

Explain the key observation or pattern that makes the solution work.

---

## Approach

Describe the algorithm step by step. Keep this focused on the recommended `solution.py` implementation.

---

## Complexity

- **Time:** `O(...)`
- **Space:** `O(...)`

---

## Edge Cases

- Mention empty inputs, single-element inputs, duplicate values, boundary values, or other cases that affect correctness.

---

## Code

The canonical implementation is in [`solution.py`](solution.py). Alternative approaches, if any, should be placed under `approaches/` and explained here.

<!--
Sections below are OPTIONAL ("AI card"). If any are filled in, this
problem must carry an `ai_card_status` entry in metadata.json
(draft | reviewed | interview-ready). See CONTRIBUTING.md section
"Optional Problem README Sections (AI Card)". Delete each unused
subsection entirely rather than leaving the placeholder text in
place.
-->

---

## Brute-force baseline

The naive solution we are improving on. State the algorithm in one sentence and its complexity.

- **Time:** `O(...)`
- **Space:** `O(...)`

---

## Common mistakes

- Off-by-one or loop-boundary mistakes specific to this problem.
- Wrong invariant ordering in the inner loop.
- (One bullet per concrete failure mode.)

---

## Failure cases

Inputs that break a naive approach but should be handled by the canonical solution. At least two distinct cases.

1. Input: `...` — naive returns `...`, correct is `...`. Why.
2. Input: `...` — ...

---

## Interview follow-ups

Plausible follow-ups an interviewer might ask and the direction the answer should go.

- "What if the input fits in memory but not in cache?"
- "Generalise to k pointers / k-sum."

---

## Bilingual summary

A short summary in both languages. Semantic parity required — no machine-translation register.

**English.** One- or two-sentence statement of the solution: the data structure used, the invariant it maintains, and the resulting complexity.

**中文。** 用同样的语义、对应的术语写一句话：使用的数据结构、维护的不变量、最终复杂度。
