# 动态规划入门（中文版）

DP 适合“有重叠子问题 + 最优子结构”的问题。

## DP 四步法

1. 定义状态 `dp[i]` / `dp[i][j]`。
2. 写状态转移。
3. 确定初始值。
4. 确定遍历顺序。

## 例子：爬楼梯

- `dp[i]`：到第 i 阶的方法数。
- 转移：`dp[i] = dp[i-1] + dp[i-2]`。
- 初值：`dp[0]=1, dp[1]=1`。

## 优化

若状态只依赖前几个位置，可滚动数组降空间。

## 易错点

- 状态定义含糊，导致转移难写。
- 初值不全。
- 遍历顺序与依赖方向冲突。
