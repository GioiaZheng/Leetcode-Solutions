# Dijkstra 最短路（中文版）

适用：**边权非负** 的单源最短路。

## 核心思想

每次确定当前“未确定节点中距离最小”的节点，并用它松弛邻边。

## 堆优化模板要点

- `dist[start]=0`
- 小根堆存 `(距离, 节点)`
- 若弹出值大于 `dist[u]`，跳过旧状态

## 复杂度

邻接表 + 堆：`O((V+E) log V)`。

## 易错点

- 有负边权不能用 Dijkstra。
- 忘记“过期堆元素”判断。
## 小例子

导航问题中，路口是点、路程是非负权重边。
从起点跑 Dijkstra，可得到到每个路口的最短距离。

