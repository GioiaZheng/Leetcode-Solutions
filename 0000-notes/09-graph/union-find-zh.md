# 并查集（Union-Find，中文版）

并查集用于高效维护“连通分量”。

## 核心操作

- `find(x)`：找根节点。
- `union(a,b)`：合并两个集合。

## 优化

- 路径压缩
- 按秩/按大小合并

## 典型应用

- 判断图是否连通
- 检测无向图成环
- Kruskal 最小生成树

## 复杂度

均摊接近 O(1)。
## 小例子

边按顺序加入无向图时：
- 若加入边 `(u,v)` 前发现 `find(u)==find(v)`，说明形成了环。

