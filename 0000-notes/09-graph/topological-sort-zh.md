# 拓扑排序（中文版）

用于有向无环图（DAG），表示“先后依赖顺序”。

## Kahn 算法（入度法）

1. 统计每个点入度。
2. 入度为 0 的点入队。
3. 出队一个点，删除其出边，更新邻居入度。
4. 新的入度 0 继续入队。

若最终处理节点数 < n，说明有环。

## 例子：课程表

- 课程是点，先修关系是有向边。
- 能处理完所有课程则可行。

## 易错点

- 边方向写反。
- 忘记初始化所有点入度（包含 0 入度点）。
