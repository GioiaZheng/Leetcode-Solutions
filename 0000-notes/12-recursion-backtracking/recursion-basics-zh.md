# 递归基础（中文版）

递归 = 函数调用自己，关键是“终止条件 + 规模缩小”。

## 三要素

1. 终止条件（何时返回）
2. 递归关系（如何拆小问题）
3. 返回值意义（每层拿到什么）

## 例子：计算阶乘

```python
def fact(n):
    if n <= 1:
        return 1
    return n * fact(n-1)
```

## 调试技巧

- 画调用栈。
- 在函数入口打印参数、出口打印返回值。

## 易错点

- 少了终止条件。
- 子问题没有缩小，导致死递归。
- 返回值语义不一致。
